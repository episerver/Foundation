@model IEnumerable<Foundation.Commerce.Personalization.RecommendedProductTileViewModel>

@if (Model != null && Model.Count() > 0)
{
    <div class="row">
        @foreach (var product in Model)
        {
            <div class="col-3">
                @Html.Partial("_Product", product.TileViewModel)
            </div>
        }
    </div>
}