namespace Foundation.Features.CatalogContent
{
    // EPiServer.Personalization.Commerce removed (no CMS 13 version).
    // Recommendation properties removed — to be re-implemented via Optimizely Graph when available.
    public interface IEntryViewModelBase
    {
    }
}
