@using Foundation.Features.MyAccount.AddressBook

@model CountryRegionViewModel

@Html.DisplayFor(x => x, new { SelectItem = Model.Region, Name = (string)ViewData["RegionName"] ?? "Address.CountryRegion.Region" })